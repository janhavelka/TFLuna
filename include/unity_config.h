#pragma once

/*
 * Project-local Unity configuration header.
 *
 * The current native test environment does not require any custom Unity
 * feature toggles, but PlatformIO's bundled Unity library includes this file
 * unconditionally when it is present on the include path.
 */
